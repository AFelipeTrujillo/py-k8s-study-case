apiVersion: v1
kind: Service
metadata:
  name: service-v3
  namespace: mysql-ns
  labels:
    app: api
    version: v3
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30083
  selector:
    app: api
    version: v3
