apiVersion: v1
kind: Service
metadata:
  name: service-v2
  namespace: mysql-ns
  labels:
    app: api
    version: v2
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30082
  selector:
    app: api
    version: v2
