apiVersion: v1
kind: Service
metadata:
  name: service-v1
  namespace: mysql-ns
  labels:
    app: api
    version: v1
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081
  selector:
    app: api
    version: v1
