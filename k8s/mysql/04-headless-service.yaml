apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: mysql-ns
  labels:
    app: mysql
spec:
  clusterIP: None
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql
